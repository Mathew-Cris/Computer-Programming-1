// here we have declaired age that holdes a value 20
let age = 20;

// ternary operator returns left operan if the condition was true
let access = age < 18 ? "denied" : "allowed";

console.log(access); // this will return the result allowed since the condition was true
